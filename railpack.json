{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "deploy": {
    "startCommand": "sh -c \"ln -sfn /app /tmp/tamilwin_scraper && PYTHONPATH=/tmp uvicorn tamilwin_scraper.fastapi_app:app --host 0.0.0.0 --port $PORT\""
  }
}
